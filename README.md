# backend-intern
# Task Manager API

## Overview
Backend API for managing user authentication and tasks with role-based access (user/admin). Built with Node.js, Express, MongoDB, and JWT.

## Features
- User registration and login (JWT authentication)
- Role-based authorization (user/admin)
- CRUD operations on tasks
- Admin can view all tasks
- Password hashing with bcrypt

## Tech Stack
- Node.js, Express.js
- MongoDB + Mongoose
- JWT for authentication
- bcrypt for password security

## Setup Instructions

1. Clone repo:
```bash
git clone https://github.com/yourusername/your-repo.git
cd your-repo
```
2. Install dependencies:
npm install

4. Start server (development):
npm run dev
Server runs at http://localhost:5000

API Documentation

Available as Swagger (http://localhost:5000/api-docs).

Do the same for Frondend
